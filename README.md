# influxdb-proxy-exporter
InfluxDB Proxy Exporter is a simple HTTP proxy that forwards InfluxDB write requests to an upstream InfluxDB instance, while also exposing the metrics on a Prometheus endpoint.
